{
  "name": "nexus-doctor",
  "version": "0.1.0",
  "description": "Read-only health checks for Nexus (CLI version, orchestrator connectivity, NTP offset, resources & threads).",
  "type": "module",
  "bin": {
    "nexus-doctor": "./nexus-doctor.mjs"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "node ./nexus-doctor.mjs --help",
    "doctor": "node ./nexus-doctor.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.34.0"
  }
}
